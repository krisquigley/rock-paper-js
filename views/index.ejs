<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/bootstrap.min.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class='col-xs-12'>
          <div class="jumbotron">
            <h1><%= title %></h1>
            <p>Welcome to <%= title %></p>
            <h2 data-behavior='countdown' style='display: none'>3</h2>
            <button data-behavior='start' onClick='startGame()' class='btn btn-success btn-lg'>Start</button>
            <form method='post' action='/' style='display: none' data-behavior='movePicker'>
              <div class="btn-group btn-group-lg" role="group" aria-label="...">
                <input type='submit' name='move' value='🗿' class='btn btn-default btn-lg'/>
                <input type='submit' name='move' value='📄' class='btn btn-default btn-lg'/>
                <input type='submit' name='move' value='✂' class='btn btn-default btn-lg'/>
              </div>
            </form>
          </div>
        </div>
      </div>
      <div data-behavior='results'>
        <div class='row text-center'>
          <div class='col-xs-4'>
            <h3><%= playerOne.name || 'You' %></h3>
          </div>
          <div class='col-xs-4'>
            <h3><small>Vs</small></h3>
          </div>
          <div class='col-xs-4'>
            <h3><%= playerTwo.name || 'Me' %></h3>
          </div>
        </div>
        <div class='row text-center'>
          <div class='col-xs-4'>
            <h4><%= playerOne.move %></h4>
          </div>
          <div class='col-xs-4'>
            &nbsp; 
          </div>
          <div class='col-xs-4'>
            <h4><%= playerTwo.move %></h4>
          </div>
        </div>
        <div class='row'>
          <div class='col-xs-12'>
            <p>
              <% if (winner === null) { %>
                Draw
              <% } else if (winner === undefined) { %>
                Stop cheating!
              <% } else if (winner) { %> 
                <%= winner.name %> won!
              <% } %>
            <p>
          </div>
        </div>
      </div>
    </div>
    <script src='/javascripts/countdown.js'></script>
    </body>
  </html>
